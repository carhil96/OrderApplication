type Order {
  id: ID!
  customer: String
  amount: Float
  status: String
  createdAt: String
}

type Query {
  orders: [Order]
  orderById(id: ID!): Order
}

type Mutation {
  createOrder(customer: String!, amount: Float!): Order
}
